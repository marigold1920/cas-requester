import React from "react";
import { View, Text, StyleSheet, FlatList } from "react-native";

const ListScreen = () => {
  return <Text>List Scrddeen</Text>;
};
const styles = StyleSheet.create({});

export default ListScreen;
