import { Dimensions, StyleSheet } from "react-native";

const styles = StyleSheet.create({
    keyboardContainer: {
        flex: 1,
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center"
    },
    container: {
        flex: 1,
        height: "100%",
        flexDirection: "column"
    },
    //css for block app_logo-na0me
    block_logo_name: {
        marginTop: 40
    },
    //css for block button
    block_button: {
        flexDirection: "column",
        alignItems: "center"
    }
});

export default styles;
